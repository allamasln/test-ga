{
  "name": "test-ga",
  "version": "1.0.0",
  "private": true,
  "description": "test-ga",
  "author": "Antonio Llamas",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "deploy:ci": "gatsby build --prefix-paths && gh-pages -d public -r  https://$GH_TOKEN@github.com/allamasln/test-ga.git"

    
  },
  "dependencies": {
    "gatsby": "^5.5.0",
    "gh-pages": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
